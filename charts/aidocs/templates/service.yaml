apiVersion: v1
kind: Service
metadata:
  name: {{ include "aidocs.fullname" . }}
  labels:
    {{- include "aidocs.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aidocs.selectorLabels" . | nindent 4 }}
